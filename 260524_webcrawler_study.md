# 파이썬으로 배우는 웹 크롤러

**날짜:** 2026-05-24  
**태그:** `Python` `Web Crawling` `HTTP` `Server` `Client`

---

## 준비 — 크롤러를 만들기 위해 필요한 것

### 1. 클라이언트

클라이언트란 웹 브라우저 및 웹을 의미한다. 우리가 사용하는 각종 앱, 프로그램, 웹 브라우저가 모두 클라이언트 프로그램이다. 기본 개념은 데이터를 요청하여 받아온 뒤 사용자에게 제공하는 것이다. 크롤러는 이 브라우저와 같은 역할을 하는 프로그램을 파이썬으로 직접 만드는 것이다.

### 2. 서버

클라이언트를 제외하고 보이지 않는 곳에서 클라이언트의 처리를 도와주는 것을 서버라고 부른다. 서버 없이 클라이언트는 새로운 데이터를 제공할 수 없으며, 특히 웹은 서버 없이는 절대 동작할 수 없다.

**서버의 종류:** 영상 제공 / 파일 / 도메인 / 전자우편 / 채팅 및 음성 / 게임 / 웹 서버

> 영상 서버는 데이터를 한 번에 전송하지 않고 일정량씩 분할하여 보내는데, 이를 **스트리밍**이라고 한다.  
> 도메인 주소에 대응하는 IP를 알려주는 서비스를 **DNS(도메인 네임 서비스)**라고 한다.  
> 웹 서버는 **정적 페이지**(항상 같은 데이터)와 **동적 페이지**(게시판처럼 데이터가 변하는 페이지) 두 가지 형태로 페이지를 제공한다.

### 3. 언어 (파이썬)

파이썬은 데이터 처리에 특화된 언어로, 인공지능과 데이터 분석 관련 라이브러리와 프레임워크가 풍부하다. 크롤러를 만들기에 적합한 이유다.

### 4. API

데이터를 수집하는 가장 간단한 방법은 API를 활용하는 것이다. API란 특정 서비스가 데이터를 제공하는 방법이다. 단, 모든 웹사이트가 API를 제공하지는 않기 때문에, API가 없는 경우 크롤러를 직접 만들어 데이터를 수집할 수 있다.

---

## 서버와 클라이언트의 데이터를 주고받는 방법

서버와 클라이언트가 데이터를 주고받는 규약을 **프로토콜**이라고 한다.

---

## 요청 메소드 (CRUD)

클라이언트가 서버에 데이터를 요청할 때 크게 네 가지 타입이 있다. 이를 **CRUD**라고 한다.

| 메소드 | CRUD | 역할 | 예시 |
|---|---|---|---|
| `GET` | Read | 데이터 요청 | 웹 페이지 불러오기, 특정 게시글 조회 |
| `POST` | Create | 데이터 추가 | 게시글 작성, 상품 등록 |
| `PUT` | Update | 데이터 수정 | 회원 정보 수정 |
| `DELETE` | Delete | 데이터 삭제 | 회원 탈퇴 |

> 크롤러에서는 데이터를 가져오는 것이 목적이므로 주로 **GET**과 **POST** 요청을 사용한다.

---

## 응답 코드

서버가 클라이언트 요청을 처리한 후 상태에 따라 반환하는 코드다.

### 1XX — 조건부 응답

클라이언트가 데이터를 일부만 포함해 보내어 서버가 데이터를 더 보내라고 하는 응답 코드다. 단, 1XX는 사용하지 않아야 한다.

| 코드 | 의미 |
|---|---|
| 100 | 계속 |
| 101 | 프로토콜 전환 |
| 102 | 처리 중 |

### 2XX — 성공

서버가 클라이언트 요청을 성공적으로 처리한 경우다. 자주 쓰이는 코드는 **200, 201, 204**다.

| 코드 | 의미 |
|---|---|
| 200 | 요청 성공 |
| 201 | 데이터 정상 저장 (POST 요청 후 반환) |
| 202 | 요청 접수됐으나 아직 처리되지 않음 |
| 203 | 정보를 신뢰할 수 없는 경우 |
| 204 | 성공했지만 반환 데이터 없음 |
| 205 | 요청 성공, 반환 콘텐츠 없음 — 클라이언트 재설정 |
| 206 | 일부 콘텐츠 |
| 207 | 다중 상태 |
| 208 | 이미 보고됨 |

### 3XX — 리다이렉션 완료

클라이언트가 요청을 완료하기 위해 추가 작업이 필요한 경우다. 자주 쓰이는 코드는 **301, 302, 303, 304, 307**이다.

| 코드 | 의미 |
|---|---|
| 300 | 여러 선택 항목 |
| 301 | 영구 이동 |
| 302 | 임시 이동 |
| 303 | 기타 위치 보기 |
| 304 | 수정되지 않음 |
| 305 | 프록시 사용 |
| 307 | 임시 새로고침 |
| 308 | 영구 새로고침 |

### 4XX — 클라이언트 오류

문제 발생 원인이 클라이언트에 있는 경우다. 자주 쓰이는 코드는 **400, 401, 403, 404, 405**다.

| 코드 | 의미 |
|---|---|
| 400 | 잘못된 요청 |
| 401 | 권한 없음 (인증 실패 — 로그인 필요) |
| 402 | 결제 필요 |
| 403 | 금지됨 (인가 실패 — 관리자 아님) |
| 404 | 찾을 수 없음 (존재하지 않는 URL) |
| 405 | 잘못된 요청 메소드 사용 |
| 406 | 허용되지 않음 |
| 407 | 프록시 인증 필요 |
| 408 | 요청 시간 초과 (timeout) |
| 409 | 충돌 |
| 410 | 사라짐 |
| 411 | 길이 필요 |
| 412 | 사전 조건 실패 |
| 413 | 요청 속성이 너무 큼 |
| 414 | 사전 조건 실패 |
| 415 | 지원되지 않는 미디어 유형 |
| 416 | 처리할 수 없는 요청 범위 |
| 417 | 예상 실패 |
| 420 | Enhance Your Calm |
| 422 | 처리할 수 없는 엔티티 |
| 423 | 잠김 |
| 424 | 메소드 실패 |
| 425 | 정렬되지 않은 컬렉션 |
| 426 | 업그레이드 필요 |
| 428 | 전제조건 필요 |
| 429 | 너무 많은 요청 |
| 431 | 요청 헤더 필드가 너무 큼 |
| 444 | 응답 없음 |
| 449 | 다시 시도 |
| 450 | 법적 문제로 이용 불가 |
| 451 | 리다이렉션 |
| 494 | 요청 헤더가 너무 큼 |
| 495 | cert 오류 (cert = 인증키) |
| 496 | cert 없음 |
| 497 | http to https |
| 499 | 클라이언트가 요청을 닫음 |

### 5XX — 서버 오류

서버의 설정이나 코드 문제로 오류가 난 경우다. 자주 쓰이는 코드는 **500, 502, 503, 504, 505**다.

| 코드 | 의미 |
|---|---|
| 500 | 내부 서버 오류 |
| 501 | 구현되지 않음 |
| 502 | 불량 게이트웨이 |
| 503 | 서버 사용 불가 |
| 504 | 게이트웨이 시간 초과 |
| 505 | HTTP 버전이 지원되지 않음 (이 경우 HTTPS 사용) |
| 506 | Variant Also Negotiates |
| 507 | 용량 부족 |
| 508 | 루프 감지 |
| 509 | 대역폭 제한 초과 |
| 510 | 확장되지 않음 |
| 511 | 네트워크 인증 필요 |
| 598 | 네트워크 읽기 시간 초과 오류 |
| 599 | 네트워크 연결 시간 초과 오류 |

---

## 헤더

서버와 클라이언트 간 데이터를 주고받을 때 포함되는 부가 정보다.

| 종류 | 설명 |
|---|---|
| 요청 헤더 | 클라이언트가 서버에 요청할 때 포함하는 헤더 |
| 응답 헤더 | 서버가 클라이언트에 응답할 때 포함하는 헤더 |
| 일반 헤더 | 양측 모두에서 사용되는 공통 헤더 |

클라이언트는 **일반 헤더 + 요청 헤더 + 엔티티 헤더** 형태로 요청을 보낸다.

---

*Study log | Python Web Crawling Series*[Uploading 260524_webcrawler_study.md…]()
# 파이썬으로 배우는 웹 크롤러

**날짜:** 2026-05-24  
**태그:** `Python` `Web Crawling` `HTTP` `Server` `Client`

---

## 준비 — 크롤러를 만들기 위해 필요한 것

### 1. 클라이언트

클라이언트란 웹 브라우저 및 웹을 의미한다. 우리가 사용하는 각종 앱, 프로그램, 웹 브라우저가 모두 클라이언트 프로그램이다. 기본 개념은 데이터를 요청하여 받아온 뒤 사용자에게 제공하는 것이다. 크롤러는 이 브라우저와 같은 역할을 하는 프로그램을 파이썬으로 직접 만드는 것이다.

### 2. 서버

클라이언트를 제외하고 보이지 않는 곳에서 클라이언트의 처리를 도와주는 것을 서버라고 부른다. 서버 없이 클라이언트는 새로운 데이터를 제공할 수 없으며, 특히 웹은 서버 없이는 절대 동작할 수 없다.

**서버의 종류:** 영상 제공 / 파일 / 도메인 / 전자우편 / 채팅 및 음성 / 게임 / 웹 서버

> 영상 서버는 데이터를 한 번에 전송하지 않고 일정량씩 분할하여 보내는데, 이를 **스트리밍**이라고 한다.  
> 도메인 주소에 대응하는 IP를 알려주는 서비스를 **DNS(도메인 네임 서비스)**라고 한다.  
> 웹 서버는 **정적 페이지**(항상 같은 데이터)와 **동적 페이지**(게시판처럼 데이터가 변하는 페이지) 두 가지 형태로 페이지를 제공한다.

### 3. 언어 (파이썬)

파이썬은 데이터 처리에 특화된 언어로, 인공지능과 데이터 분석 관련 라이브러리와 프레임워크가 풍부하다. 크롤러를 만들기에 적합한 이유다.

### 4. API

데이터를 수집하는 가장 간단한 방법은 API를 활용하는 것이다. API란 특정 서비스가 데이터를 제공하는 방법이다. 단, 모든 웹사이트가 API를 제공하지는 않기 때문에, API가 없는 경우 크롤러를 직접 만들어 데이터를 수집할 수 있다.

---

## 서버와 클라이언트의 데이터를 주고받는 방법

서버와 클라이언트가 데이터를 주고받는 규약을 **프로토콜**이라고 한다.

---

## 요청 메소드 (CRUD)

클라이언트가 서버에 데이터를 요청할 때 크게 네 가지 타입이 있다. 이를 **CRUD**라고 한다.

| 메소드 | CRUD | 역할 | 예시 |
|---|---|---|---|
| `GET` | Read | 데이터 요청 | 웹 페이지 불러오기, 특정 게시글 조회 |
| `POST` | Create | 데이터 추가 | 게시글 작성, 상품 등록 |
| `PUT` | Update | 데이터 수정 | 회원 정보 수정 |
| `DELETE` | Delete | 데이터 삭제 | 회원 탈퇴 |

> 크롤러에서는 데이터를 가져오는 것이 목적이므로 주로 **GET**과 **POST** 요청을 사용한다.

---

## 응답 코드

서버가 클라이언트 요청을 처리한 후 상태에 따라 반환하는 코드다.

### 1XX — 조건부 응답

클라이언트가 데이터를 일부만 포함해 보내어 서버가 데이터를 더 보내라고 하는 응답 코드다. 단, 1XX는 사용하지 않아야 한다.

| 코드 | 의미 |
|---|---|
| 100 | 계속 |
| 101 | 프로토콜 전환 |
| 102 | 처리 중 |

### 2XX — 성공

서버가 클라이언트 요청을 성공적으로 처리한 경우다. 자주 쓰이는 코드는 **200, 201, 204**다.

| 코드 | 의미 |
|---|---|
| 200 | 요청 성공 |
| 201 | 데이터 정상 저장 (POST 요청 후 반환) |
| 202 | 요청 접수됐으나 아직 처리되지 않음 |
| 203 | 정보를 신뢰할 수 없는 경우 |
| 204 | 성공했지만 반환 데이터 없음 |
| 205 | 요청 성공, 반환 콘텐츠 없음 — 클라이언트 재설정 |
| 206 | 일부 콘텐츠 |
| 207 | 다중 상태 |
| 208 | 이미 보고됨 |

### 3XX — 리다이렉션 완료

클라이언트가 요청을 완료하기 위해 추가 작업이 필요한 경우다. 자주 쓰이는 코드는 **301, 302, 303, 304, 307**이다.

| 코드 | 의미 |
|---|---|
| 300 | 여러 선택 항목 |
| 301 | 영구 이동 |
| 302 | 임시 이동 |
| 303 | 기타 위치 보기 |
| 304 | 수정되지 않음 |
| 305 | 프록시 사용 |
| 307 | 임시 새로고침 |
| 308 | 영구 새로고침 |

### 4XX — 클라이언트 오류

문제 발생 원인이 클라이언트에 있는 경우다. 자주 쓰이는 코드는 **400, 401, 403, 404, 405**다.

| 코드 | 의미 |
|---|---|
| 400 | 잘못된 요청 |
| 401 | 권한 없음 (인증 실패 — 로그인 필요) |
| 402 | 결제 필요 |
| 403 | 금지됨 (인가 실패 — 관리자 아님) |
| 404 | 찾을 수 없음 (존재하지 않는 URL) |
| 405 | 잘못된 요청 메소드 사용 |
| 406 | 허용되지 않음 |
| 407 | 프록시 인증 필요 |
| 408 | 요청 시간 초과 (timeout) |
| 409 | 충돌 |
| 410 | 사라짐 |
| 411 | 길이 필요 |
| 412 | 사전 조건 실패 |
| 413 | 요청 속성이 너무 큼 |
| 414 | 사전 조건 실패 |
| 415 | 지원되지 않는 미디어 유형 |
| 416 | 처리할 수 없는 요청 범위 |
| 417 | 예상 실패 |
| 420 | Enhance Your Calm |
| 422 | 처리할 수 없는 엔티티 |
| 423 | 잠김 |
| 424 | 메소드 실패 |
| 425 | 정렬되지 않은 컬렉션 |
| 426 | 업그레이드 필요 |
| 428 | 전제조건 필요 |
| 429 | 너무 많은 요청 |
| 431 | 요청 헤더 필드가 너무 큼 |
| 444 | 응답 없음 |
| 449 | 다시 시도 |
| 450 | 법적 문제로 이용 불가 |
| 451 | 리다이렉션 |
| 494 | 요청 헤더가 너무 큼 |
| 495 | cert 오류 (cert = 인증키) |
| 496 | cert 없음 |
| 497 | http to https |
| 499 | 클라이언트가 요청을 닫음 |

### 5XX — 서버 오류

서버의 설정이나 코드 문제로 오류가 난 경우다. 자주 쓰이는 코드는 **500, 502, 503, 504, 505**다.

| 코드 | 의미 |
|---|---|
| 500 | 내부 서버 오류 |
| 501 | 구현되지 않음 |
| 502 | 불량 게이트웨이 |
| 503 | 서버 사용 불가 |
| 504 | 게이트웨이 시간 초과 |
| 505 | HTTP 버전이 지원되지 않음 (이 경우 HTTPS 사용) |
| 506 | Variant Also Negotiates |
| 507 | 용량 부족 |
| 508 | 루프 감지 |
| 509 | 대역폭 제한 초과 |
| 510 | 확장되지 않음 |
| 511 | 네트워크 인증 필요 |
| 598 | 네트워크 읽기 시간 초과 오류 |
| 599 | 네트워크 연결 시간 초과 오류 |

---

## 헤더

서버와 클라이언트 간 데이터를 주고받을 때 포함되는 부가 정보다.

| 종류 | 설명 |
|---|---|
| 요청 헤더 | 클라이언트가 서버에 요청할 때 포함하는 헤더 |
| 응답 헤더 | 서버가 클라이언트에 응답할 때 포함하는 헤더 |
| 일반 헤더 | 양측 모두에서 사용되는 공통 헤더 |

클라이언트는 **일반 헤더 + 요청 헤더 + 엔티티 헤더** 형태로 요청을 보낸다.

서버는 일반 헤더 + 응답 헤더 + 엔티티 헤더의 형태로 응답한다.

**요청 헤더:** 클라이언트가 문서와 구성 형태 및 형식을 포함한 헤더다. 클라이언트의 주요 헤더가 된다.

| 헤더 | 설명 |
|---|---|
| Accept | 클라이언트의 사용 가능 미디어 타입 |
| Accept-Charset | 클라이언트에서 사용할 수 있는 문자 집합 |
| Accept-Encoding | 클라이언트에서 제공되는 인코딩 방법(압축) |
| Accept-Language | 클라이언트가 인식할 수 있는 언어 |
| Host | 서버의 기본 URL |
| If-Match | ETag값 비교 후 메소드 수행 |
| If-None-Match | ETag값 비교 후 다를 때 → method 수행 |
| If-Unmodified-Since | 헤더값에 지정된 날짜로부터 수정이 없는 경우 메소드 수행 |
| Max-Forwards | 이 메시지가 거쳐갈 수 있는 최대 Proxy의 개수 지정 |
| Proxy-Authorization | 비공개 프록시 서버 유저 인증을 위한 코드 |
| Range | 자원의 일부분만 받을 때(이어받기 가능) 받을 범위 지정 |
| From | 메일 지정 |
| Cookie | 쿠키 정보 |
| Referer | 클라이언트가 서버에 요청하기 직전에 머물렀던 주소 (크롤러에서 이 부분을 만들어 줘야 하는 경우가 있음) |
| User-Agent | 브라우저 정보 (크롬, 사파리, 익스플로러, 파이어폭스 등) |
| Method | 요청 메소드 |
| HTTP-Version | HTTP 버전 |
| Request-URI | 요청 데이터의 절대 주소, 상대 주소 |
| Authorization | 사용자 인증 정보 |

**응답 헤더:** 서버가 클라이언트에 응답할 때 요청과 서버의 구성을 포함한 헤더다.

| 헤더 | 설명 |
|---|---|
| HTTP-Version | HTTP 버전 |
| Status-code | 응답 코드 |
| Reason-Phrase | 응답 코드에 대한 간략한 설명 |
| Location | 요구한 정보의 실제 위치 |
| Server | 서버 프로그램의 이름과 버전 정보 |
| WWW-Authenticate | 사용자 인증이 필요한 자원 요구 시 서버가 제공하는 인증 방식 |
| Age | 요구 후 원 서버에서 응답을 생성하기까지의 시간 |
| Proxy-Authenticate | 프록시 서버일 경우 유저 인증을 요구하기 위한 헤더 |
| Public | 서버에서 지원 가능한 메소드 리스트 |
| Retry-After | 503 에러 시 해당 시간만큼 기다리라는 의미 |
| Warning | 상태 코드와 응답 구문에 추가적인 경고 |
| Set-Cookie | 서버측에서 세션 쿠키 정보 설정 |
| Expires | 캐시 유효 기간 |
| Last-Modified | 리소스 마지막 수정날 |

**일반 헤더:** 서버와 클라이언트 양측 모두에서 사용되는 공통 헤더다.

| 헤더 | 설명 |
|---|---|
| Date | 현재 시간 |
| Pragma | 캐시 제어 (HTTP 1.1에서는 사용 안 함) |
| Cache-Control | 캐시 유무, 업데이트 시간, 내용, 지움 등의 정보 |
| Connection | 연결 지속 |
| Transfer-Encoding | [entity-body]의 압축 방식 |
| Upgrade | 프로토콜 변경 시 |
| Via | 중계 서버 (프록시, 게이트웨이 등) |

**엔티티 헤더:** 메시지에 대한 설명을 포함하는 헤더다.

| 헤더 | 설명 |
|---|---|
| Content-Encoding | [entity-body]의 리소스 압축 방식 (gzip, compress, deflate 등) |
| Content-Length | [entity-body]의 리소스 크기 (단위: Byte) |
| Content-Type | [entity-body]의 미디어 타입 |

> 서버와 클라이언트는 상황에 따라 필요한 정보만 포함하여 헤더를 만든다.

---

## 5. URL

데이터를 보내기 위해서는 URL 구조를 알아야 한다. URL 구조를 얼마나 잘 파악하느냐에 따라 효율적인 크롤러를 만들 수 있다.

URL은 **Uniform Resource Locator**의 약자로, 네트워크상에서 자원을 요청하는 규약이다. 웹 브라우저뿐 아니라 스마트폰 앱, 컴퓨터 응용 프로그램들도 서버와 데이터를 주고받을 때 URL을 이용한다.

### 5-1. URL 구조

```
프로토콜://주소 또는 IP:포트번호/리소스 경로?쿼리스트링
```

| 구성 요소 | 설명 |
|---|---|
| 프로토콜 | 데이터를 주고받는 규약. 주로 http 사용 |
| 주소/IP | 데이터를 요청하는 타겟 |
| 포트번호 | 목적지 컴퓨터에서 어떤 프로그램인지 알려줌. 프로토콜에 따라 생략 가능 |
| 리소스 경로 | 한 사이트에서 페이지를 옮길 때마다 다르게 표현됨 |
| 쿼리스트링 | 클라이언트가 서버에 보내는 데이터 |

**프로토콜별 포트 번호**

| 프로토콜 | 포트 번호 | 용도 |
|---|---|---|
| HTTP | 80 | 웹 |
| HTTPS | 443 | 보안 웹 |
| FTP | 20, 21 | 파일 전송 |
| Telnet | 23 | 원격 접속 |
| SSH | 22 | 원격 접속 (보안) |

### 5-2. 리소스 경로

주소 다음에 슬래시(`/`)로 시작하는 부분이다. 클라이언트가 요청하는 리소스의 경로가 포함되며, 데이터를 함께 보낼 수 있다.

```
A제품: http://www.example.com/product/1
B제품: http://www.example.com/product/2
C제품: http://www.example.com/product/3
```

이 경우 1, 2, 3이 데이터에 해당한다.

### 5-3. 쿼리스트링

리소스 경로 이후 물음표(`?`)로 구분되며, `변수=데이터&변수=데이터` 형태로 데이터를 전송한다.

```
A제품: http://www.example.com/product?pro=1&cate=2
B제품: http://www.example.com/product?pro=2&cate=2
C제품: http://www.example.com/product?pro=3&cate=3
```

### 5-4. Header Body

POST 요청을 하게 되면 데이터를 Body에 실어 보낸다. 리소스 경로나 쿼리스트링으로 데이터를 보내면 한계가 있지만, Header의 Body를 이용하면 더 많은 데이터를 보낼 수 있다. 단, 이 경우 URL로는 확인이 불가능하다.

1. 웹 페이지에서 마우스 오른쪽 버튼 클릭 → **검사** 클릭
2. 상단 탭에서 **[Network]** 클릭
3. Name 리스트에서 항목을 클릭하면 오른쪽에 상세 정보가 나타남

| 항목 | 설명 |
|---|---|
| Form Data | Body 데이터 (POST 요청일 때만 존재) |
| Query String Parameters | 쿼리스트링 데이터 |

> Request Method가 POST일 때만 Body에 데이터가 존재한다.  
> 웹 페이지 접속, 새로고침 같은 일반적인 접속은 GET 요청이라 Body 데이터가 비어있다.  
> POST 요청이라면 반드시 Body 데이터를 확인해야 한다.  
> URL이 변하지 않는 동작이 있다면 **[개발자 도구] → [Network] 탭**을 확인해야 한다.


