# Java 기초 - 프로그래밍 언어와 자바 / 개발 환경 / 개발 과정

**2026.07.09**  
`Java` `JDK` `JVM` `Eclipse`

---

## 1. 프로그래밍 언어와 자바

| 용어 | 설명 |
|---|---|
| 기계어 | 컴퓨터(운영체제)가 이해하고 실행할 수 있는 0과 1로 이루어진 코드 |
| 프로그래밍 언어 | 사람의 언어와 기계어 사이의 다리 역할. C, C++, Java, Python 등 |
| 소스 파일 | 프로그래밍 언어로 작성된 파일 |
| 컴파일 | 소스 파일을 기계어로 번역하는 것. 이를 담당하는 소프트웨어를 컴파일러라고 함 |
| JDK | 자바 개발 도구. 실행 환경(JVM)과 개발 도구(컴파일러) 등을 제공 |
| 환경 변수 | 운영체제가 실행하는 데 필요한 정보를 제공해주는 변수 |

> JDK 설치 후 명령 라인에서 `javac`, `java` 명령어를 사용하려면 `JAVA_HOME` 환경 변수를 등록하고 `Path` 환경 변수를 수정해야 함.

---

## 2. 이클립스 개발 환경

| 용어 | 설명 |
|---|---|
| 이클립스 | 무료 오픈 소스 통합 개발 환경(IDE) |
| IDE | 프로젝트 생성, 자동 코드 완성, 디버깅 등 개발에 필요한 기능을 통합 제공하는 툴 |
| 워크스페이스 | 이클립스 실행 관련 메타 데이터와 프로젝트 폴더가 저장되는 폴더 |
| 뷰 | 이클립스 내부에서 사용되는 작은 창 |
| 퍼스펙티브 | 개발에 유용한 뷰들을 미리 묶어 이름을 붙여 놓은 것 |

---

## 3. 자바 프로그램 개발 과정

```
소스 파일(.java) → javac 컴파일 → 바이트 코드 파일(.class) → JVM 실행 → 기계어
```

| 용어 | 설명 |
|---|---|
| 바이트 코드 파일 | 자바 소스 파일을 `javac` 명령어로 컴파일한 파일 (`.class`) |
| JVM | 바이트 코드 파일을 운영체제를 위한 완전한 기계어로 번역하고 실행. `java` 명령어로 구동 |
| 클래스 선언 | 자바 소스 파일은 클래스 선언부와 클래스 블록으로 구성 |
| main() 메소드 | 프로그램 실행 진입점. `java` 명령어 실행 시 제일 먼저 찾아 블록 내부를 실행 |
| 주석 | 코드에 붙이는 설명. 컴파일 과정에서 무시되며 바이트 코드로 번역되지 않음 |
| 실행문 | 변수 선언, 값 저장, 메소드 호출에 해당하는 코드. 끝에 세미콜론(`;`) 필수 |
